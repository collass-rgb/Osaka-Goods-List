[build]
builder = "nixpacks"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
```

---

## 同時確認 `requirements.txt` 內容正確

GitHub 上打開 `requirements.txt`，確認內容**只有這三行**，沒有多餘空白或奇怪字元：
```
discord.py[voice]==2.5.0
aiohttp>=3.9.0
anthropic>=0.40.0
